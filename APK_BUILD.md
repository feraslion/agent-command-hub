# إعداد إصدار Android وAPK

## الملفات المهيأة

يتضمن المشروع الآن مجلد `android/` الأصلي، وإعدادات `eas.json` التي تحتوي ملف تعريف **apk** لإنتاج حزمة قابلة للتثبيت داخلياً. معرّف حزمة Android هو `com.app.agentcommandhub`، والحد الأدنى المدعوم هو Android SDK 24، ومعماريات الإصدار هي `armeabi-v7a` و`arm64-v8a`.

| العنصر | القيمة |
| --- | --- |
| اسم التطبيق | Agent Command Hub |
| معرّف الحزمة | `com.app.agentcommandhub` |
| `versionCode` | 1 |
| الحد الأدنى لـ SDK | 24 |
| ملف APK الداخلي | `apk` في `eas.json` |
| إصدار المتجر | `production` وينتج AAB |

## أسلوب الإصدار

استخدم زر **Publish** في واجهة المشروع لإنشاء حزمة Android. تتولى خدمة النشر إدارة بيانات التوقيع المطلوبة للإصدار؛ لذلك لا تُنشئ أو ترفع ملف keystore يدوياً إلى المستودع. ملفات `*.jks` وملفات المفاتيح الخاصة محمية من الإدراج في Git عبر `.gitignore`.

> ملف APK مناسب للتثبيت الداخلي والاختبار. أما الرفع إلى Google Play فيتطلب إصدار AAB موقّعاً عبر ملف تعريف `production`.

## اختبار داخلي من الهاتف

عند حفظ نسخة مشروع ناجحة، استخدم زر **Publish** في واجهة المشروع واختر ملف تعريف `apk`. ينتج المسار حزمة داخلية قابلة للتثبيت ولا يتطلب وضع مفتاح إنتاجي في Git أو في الهاتف. بعد حصولك على رابط الحزمة، ثبّتها على جهاز Android حقيقي ثم نفّذ اختبار الدخان التالي.

| التدفق | النتيجة المقبولة |
| --- | --- |
| فتح التطبيق وتسجيل الدخول | تظهر لوحة التحكم مع السمة المطابقة لإعداد الجهاز. |
| فتح المشاريع والموافقات وRuntime | لا يوجد توقف أو شاشة بيضاء، وتظهر حالة Runner بوضوح. |
| بوابة إقران Runner | يمكن إنشاء إعداد الإقران ونسخه، من دون بدء تنفيذ أو كشفه في السجل. |
| بحث وتشاور | يبقى البحث الذاتي معطلاً حتى يفعّله المالك داخل حملة مشروع. |
| الإشعارات | يظهر طلب الإذن عند الحاجة، ولا يمنع رفضه استعمال التطبيق. |

### توقيع الإصدار

لا يستخدم اختبار APK الداخلي keystore إنتاجياً مخزناً في المستودع. ملف `android/app/debug.keystore` مخصص للتطوير فقط، أما أسرار الإصدار الخارجي فتدار خارج Git. يحمي `.gitignore` امتدادات `*.jks` و`*.keystore` وملفات `keystore.properties` لمنع رفعها بالخطأ. قبل أي توزيع خارجي، استخدم ملف تعريف `production` لإنتاج AAB، واحتفظ بمادة التوقيع في إدارة أسرار معتمدة فقط.
